#' @param choicerule (discrete responses only) A string, the choice rule.  `cm_choicerules()` shows the allowed values. `"none"` is no choice rule, `"softmax"` is [soft-maximum](https://en.wikipedia.org/wiki/Softmax_function), `"luce"` is [Luce's axiom](https://en.wikipedia.org/wiki/Luce\%27s_choice_axiom).
#' 

